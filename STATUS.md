# STATUS — AnimaXS (keep short & current)

- **Current milestone:** Full production inference pipeline, generation architecture (K002), real checkpoint resume (I004), lifecycle/cancel/background (K003), memory/thermal policy (K004), ModelStore production integration (D006), complete app UI (K001), and diagnostics (K005) are implemented and CI-validated. A005 license audit RESOLVED for non-commercial redistribution. Remaining: L001 full-inference regression thresholds, full-inference workflow fix, L002 release (non-commercial) if packs publishable, L003 manual run, docs (README/TEST_MATRIX/DEVICE_TESTS/handoff), and physical A12 acceptance.
- **Current task:** Correct L001 model topology + regression assertions and repair the full-inference workflow; then L002/L003 and documentation.
- **CI baseline:** `31625249377` — 151 tests, 13 skipped, 0 failures, all three jobs green.
- **What works (2026-08-12):** K001 full UI (models/download/import/repair, prompt, seed+Randomize, Generate/Cancel/Resume, progress step/block+elapsed, image+Share, Diagnostics); K002 3-pack generation architecture (seed→RNG, stage lifetime, block progress, recoverable Metal, orchestration tests); I004 real checkpoint resume (sampler startStep, CheckpointStore, equivalence tests); K003 lifecycle (background safe-cancel + foreground resume); K004 memory warning + thermal policy; ModelStore repair/3-pack/import (D006); J003 production primitive coverage; K005 diagnostics engine + JSON export; full production pipeline (Qwen/adapter/DiT/sampler/VAE) CI-validated.
- **A005 license (commit + D069):** non-commercial redistribution of the three converted/quantized packs is permitted with required license copies + notices (`MODEL_LICENSE.md`, `MODEL_NOTICE.txt`, `docs/model-licenses/`). Commercial use remains blocked without separate licenses.
- **Known incomplete:** L001 needs measured final-latent/RGB regression thresholds from a real full-pack run; `full-inference.yml` does not yet download/verify packs; L002 release not yet published (awaiting model-assets-v1 upload decision); L003 manual full-inference run pending. Physical A12 acceptance deferred.
- **Device-only unknowns:** A12 speed, memory/jetsam, Apple5 behavior, watchdog limits, page cache, thermal stability, second-generation stability. Hosted Metal functional success does not answer them.
- **Next three tasks:**
  1. Correct L001 (3-pack topology + latent/RGB regression assertions) and repair `full-inference.yml`.
  2. Publish/verify L002 `model-assets-v1` (non-commercial) if pack files are available; run L003.
  3. Physical A12 acceptance — build in Xcode, install on iPhone XS Max, record timings/memory/thermal.
